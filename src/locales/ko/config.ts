import { item } from './item';
import { lobby } from './lobby';
import { message } from './message';
import { sys } from './sys';

export const koConfig = {
  lobby: lobby,
  message: message,
  item: item,
  sys: sys,
};
